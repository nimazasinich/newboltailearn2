# Persian Legal AI - Environment Configuration
# Copy this file to .env and configure your values

# =============================================================================
# SECURITY (REQUIRED IN PRODUCTION)
# =============================================================================

# JWT Secret - Minimum 32 characters required for production
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_minimum_32_characters_required

# Session Secret - Minimum 32 characters required for production
# Generate with: openssl rand -base64 32
SESSION_SECRET=your_session_secret_here_minimum_32_characters_required

# CSRF Secret - Optional but recommended for production
# Generate with: openssl rand -base64 32
CSRF_SECRET=your_csrf_secret_here_optional_but_recommended

# =============================================================================
# HUGGINGFACE API TOKEN (Base64 encoded)
# =============================================================================

# Encode your token using:
# Linux/macOS: echo -n "hf_your_token_here" | base64
# PowerShell: [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("hf_your_token_here"))
# 
# Encoded token: aGZfWk5MekFqY2FHYkJQQldFUlBhVHhpbklVZlFhWUFwd2JlZA==
HF_TOKEN_B64=

# Legacy support (deprecated - use HF_TOKEN_B64 instead)
HF_TOKEN_ENC=

# =============================================================================
# RATE LIMITING (Production Values)
# =============================================================================

# Global rate limit (requests per minute)
RATE_LIMIT_GLOBAL=100

# Authentication rate limit (login attempts per minute)
RATE_LIMIT_AUTH=5

# API rate limit (API calls per minute)
RATE_LIMIT_API=30

# Training rate limit (training requests per minute)
RATE_LIMIT_TRAINING=10

# Download rate limit (download requests per minute)
RATE_LIMIT_DOWNLOAD=20

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Demo mode - shows demo data instead of real data
DEMO_MODE=false

# Use fake data for development/testing
USE_FAKE_DATA=false

# Skip CSRF protection (development only)
SKIP_CSRF=false

# Use worker threads for training
USE_WORKERS=true

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Node environment
NODE_ENV=development

# Server port
PORT=3001

# CORS origin (for development)
CORS_ORIGIN=http://localhost:5173

# Database path
DATABASE_PATH=./persian_legal_ai.db

# =============================================================================
# DEVELOPMENT AUTHENTICATION (Non-production only)
# =============================================================================

# Development admin credentials (only works when NODE_ENV != production)
DEV_ADMIN_USER=admin
DEV_ADMIN_PASS=Admin123!@#

# Default admin password for database initialization
DEFAULT_ADMIN_PASSWORD=Admin123!@#

# =============================================================================
# MONITORING & LOGGING
# =============================================================================

# Enable metrics collection
ENABLE_METRICS=true

# Enable log shipping
ENABLE_LOG_SHIPPING=false

# Log shipping URL (if enabled)
LOG_SHIPPING_URL=

# =============================================================================
# PRODUCTION SECURITY CHECKLIST
# =============================================================================
# 
# Before deploying to production, ensure:
# 1. JWT_SECRET is set to a secure random string (32+ characters)
# 2. SESSION_SECRET is set to a secure random string (32+ characters)
# 3. CSRF_SECRET is set to a secure random string (32+ characters)
# 4. NODE_ENV is set to "production"
# 5. HF_TOKEN_B64 contains your valid HuggingFace token (base64 encoded)
# 6. All rate limits are configured appropriately
# 7. CORS_ORIGIN is set to your production domain
# 8. DEMO_MODE is set to false
# 9. USE_FAKE_DATA is set to false
# 10. SKIP_CSRF is set to false
#
# =============================================================================